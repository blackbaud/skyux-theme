{
  "name": "@skyux/theme",
  "version": "3.3.0",
  "description": "SKY UX Theme",
  "main": "bundles/bundle.umd.js",
  "module": "index.ts",
  "scripts": {
    "skyux": "skyux",
    "test": "npm run build:dev-bundle && skyux test",
    "test:ci": "npm run build:dev-bundle && skyux test --platform travis --coverage library && npm run visual && npm run build",
    "build": "skyux build-public-library && node ./scripts/prepare-package.js",
    "build:ci": "npm run build",
    "build:dev-bundle": "npm run build && node ./scripts/dev-bundle.js",
    "start": "npm run build:dev-bundle && skyux serve",
    "visual": "npm run build:dev-bundle && skyux e2e"
  },
  "keywords": [],
  "author": "Blackbaud",
  "license": "ISC",
  "peerDependencies": {
    "@angular/core": ">=4.3.6"
  },
  "dependencies": {
    "@blackbaud/skyux-design-tokens": "0.0.10",
    "@types/fontfaceobserver": "0.0.5",
    "fontfaceobserver": "2.0.9",
    "normalize-scss": "5.0.3"
  },
  "devDependencies": {
    "@blackbaud/skyux": "2.30.0",
    "@blackbaud/skyux-builder": "1.29.0",
    "@skyux-sdk/builder-plugin-skyux": "1.0.0-rc.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/blackbaud/skyux-theme.git"
  },
  "bugs": {
    "url": "https://github.com/blackbaud/skyux-theme/issues"
  }
}
